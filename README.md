# open-aiops
open-aiops is a lightweight, local telemetry engine built to trace and audit multi-agent AI workflows. Use a single @track_agent decorator to capture inputs, outputs, errors, and precise latencies across frameworks like LangGraph or CrewAI. Features a FastAPI ingestion core and a live Streamlit dashboard to stop infinite token loops.
